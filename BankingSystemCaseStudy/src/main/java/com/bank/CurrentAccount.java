public class CurrentAccount extends Account {
    public CurrentAccount(Database database) {
        super(database);
    }

    // Implement additional methods specific to CurrentAccount if needed
}
